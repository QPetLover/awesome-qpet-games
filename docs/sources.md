
# Sources

This archive uses source IDs from `metadata/sources.yml`.

Private QQ community sources should remain private by default. Record them as community backups without publishing private identifiers.
